{
  "name": "checkout",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "APP_NAME=checkout NODE_ENV=dev npx playwright test",
    "test:ui": "APP_NAME=checkout NODE_ENV=dev npx playwright test --ui",
    "test:debug": "APP_NAME=checkout NODE_ENV=dev PWDEBUG=1 npx playwright test",
    "report": "npx playwright show-report reports/playwright-report",
    "test:display": "APP_NAME=checkout NODE_ENV=dev npx playwright test --headed --workers=1"
  }
}
